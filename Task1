<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Landing Page</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    a {
      text-decoration: none;
    }

    h1, h2 {
      margin: 0 0 10px;
    }

    .hero {
      background: #d154b2;
      color: white;
      padding: 100px 20px;
      text-align: center;
    }

    .hero .btn {
      display: inline-block;
      background: white;
      color: #4a90e2;
      padding: 10px 20px;
      border-radius: 5px;
      margin-top: 20px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s ease;
    }

    .hero .btn:hover {
      background: #4a90e2;
      color: white;
    }

    .features {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      padding: 60px 20px;
      background: #f4f4f4;
    }

    .feature {
      flex: 1 1 250px;
      max-width: 300px;
      margin: 20px;
      background: white;
      padding: 20px;
      border-radius: 8px;
      text-align: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      transition: all 0.3s ease;
    }

    .feature:hover {
      transform: translateY(-5px);
      box-shadow: 0 4px 10px rgba(0,0,0,0.15);
    }

    footer {
      text-align: center;
      padding: 20px;
      background: #a8d6e4;
      color: #272626;
    }

    @media (max-width: 600px) {
      .hero {
        padding: 60px 15px;
      }
    }
  </style>
</head>
<body>

  <!-- Hero Section -->
  <section class="hero">
    <a href="#" class="btn">Task 1</a>
    <h1>Welcome to FlexSite</h1>
    <p>Your one-stop solution for clean and responsive web layouts.</p>
    <a href="#" class="btn">Get Started</a>
  </section>

  <!-- Features Section -->
  <section class="features">
    <div class="feature">
      <h2>⚡ Fast</h2>
      <p>Optimized for performance and speed.</p>
    </div>
    <div class="feature">
      <h2>📱 Responsive</h2>
      <p>Looks great on all devices.</p>
    </div>
    <div class="feature">
      <h2>🔒 Secure</h2>
      <p>Built with best security practices.</p>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <p>&copy; 2025 FlexSite. All rights reserved.</p>
  </footer>

</body>
</html>
